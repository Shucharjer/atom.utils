#pragma once

namespace atom::utils {

template <typename>
class delegate;

class basic_sink;

template <typename, typename>
class sink;

template <typename>
class dispatcher;

} // namespace atom::utils
